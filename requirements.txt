# ==================================================
# TripAI 项目依赖
# 安装: pip install -r requirements.txt
# ==================================================

# ---- LLM ----
langchain-openai>=0.2            # OpenAI 兼容 LLM SDK（支持 DeepSeek）
langgraph>=0.2                   # 工作流编排引擎

# ---- Web 框架 ----
fastapi>=0.110                   # API 服务框架
uvicorn[standard]>=0.27          # ASGI 服务器
streamlit>=1.32                  # 前端界面

# ---- 配置 & 数据 ----
pydantic-settings>=2.0           # 配置管理（.env 加载）

# ---- 日志 ----
loguru>=0.7                      # 结构化日志

# ---- 重试 & 缓存 ----
tenacity>=8.0                    # 指数退避重试
diskcache>=5.6                   # SQLite 本地缓存

# ---- HTTP ----
httpx>=0.27                      # 异步 HTTP 客户端
requests>=2.31                   # 同步 HTTP 客户端

# ---- 持久化 ----
redis>=5.0                       # Redis 状态存储
psycopg>=3.1                     # PostgreSQL 驱动

# ---- 本地知识库 ----
chromadb>=0.5                    # Chroma 向量检索 (本地模式)
sentence-transformers>=2.2       # Chroma 本地嵌入模型 (all-MiniLM-L6-v2)

# ---- 搜索 ----
duckduckgo-search>=5.0           # DuckDuckGo 搜索

# ---- MCP 天气 (可选) ----
mcp>=1.0                         # MCP 协议工具调用
pypinyin>=0.50                   # 中文转拼音

# ---- 系统监控 (health check) ----
psutil>=5.9                      # CPU/内存监控

# ---- MCP 天气模块额外依赖 ----
aiohttp>=3.9                     # 异步 HTTP (weather_client_mcp)
python-dotenv>=1.0               # .env 加载 (MCP 模块)

# ---- 前端额外依赖 ----
pandas>=2.0                      # 数据处理 (streamlit_app)
